/*
 * HomeWeatherApp  Messages
 *
 * This contains all the text for the HomeWeatherApp component.
 */

import { defineMessages } from "react-intl";

export const scope = "HomeWeatherApp";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "title",
  }
});
